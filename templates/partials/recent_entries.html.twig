<section>
    <h3>最新記事</h3>
    <ul class="posts">
    {% for p in page.find('/blog').children.order('date', 'desc').slice(0, 5) %}
    {% set bannerimage = p.media.images|first %}
    <li>
        <article>
           <header>
         <h3><a href="{{p.url}}">{{ p.title }}</a></h3>
        <time class="published" datetime="{{p.date|date("Y-m-d")}}">{{ p.date|date("Y-m-d")}}</time>
        </header>
       </article>
    </li>
   {% endfor %}
   </ul>
</section>
